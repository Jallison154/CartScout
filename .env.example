# CartScout – copy to .env and set in production

# Server
PORT=4000
NODE_ENV=development

# Database (SQLite file path; use :memory: for in-memory)
DATABASE_PATH=./data/cartscout.sqlite

# JWT – REQUIRED in production: long random strings (min 32 chars). Server will not start if NODE_ENV=production and these are missing or default.
JWT_ACCESS_SECRET=your-access-secret-min-32-chars
JWT_REFRESH_SECRET=your-refresh-secret-min-32-chars
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Mobile app – API base URL (Expo). Use your server IP when API is on another machine.
EXPO_PUBLIC_API_URL=http://localhost:4000
# Example for server on LAN: EXPO_PUBLIC_API_URL=http://192.168.10.47:4000

# Optional: use mock API so you can develop UI without a server (see docs/DEVELOPMENT.md).
# EXPO_PUBLIC_USE_MOCK_API=true

# --- Production: serve the API over HTTPS only; never send tokens over plain HTTP. ---
